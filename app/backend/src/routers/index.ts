import teamsRouter from './team.router';
import userRouter from './user.router';
import matchRouter from './match.router';

const routes = { teamsRouter, userRouter, matchRouter };

export default routes;
